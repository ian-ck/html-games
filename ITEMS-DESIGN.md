# 아이템 · 캐릭터 설정 설계안 (**미구현**)

> ⚠️ **이 문서는 설계안이고, 상당 부분은 이미 구현됐다.**
> 구현된 것(의상·코인·상점·장비 12종·소비템 2종·서버 지갑)은
> [`ARCHITECTURE.md`](ARCHITECTURE.md)가 정본이다. 아래는 **아직 안 만든 것**과
> **계산해서 알아낸 함정**을 위해 남긴다.
>
> 미구현 — 의상 세트 효과(정장 → 임원 위장, 고어룩 → 아웃사이더) ·
> 트레이드오프 아이템(기계식 키보드·텀블러·런닝화) · Claude 의 '버그 추가' 대가 ·
> 모자(애셋 품질로 보류) · 초상화.
> 대화로 정한 결정과 **계산해서 알아낸 함정**을 남긴다. 코드만 봐서는 안 보이는 것들이다.

## 왜 이 문서가 필요한가

아이템 효과를 눈대중으로 붙이면 대부분 **점수 계산에서 무의미하거나, 반대로 순위표를 망가뜨린다.**
아래 계산은 실제 코드 상수를 넣어 뽑은 것이다. 새 아이템을 만들 때 같은 계산을 먼저 할 것.

---

## 1. 애셋 — PNG 추가 0장으로 갈 수 있다

`leave-on-time/assets/characters/character{1,2,3}.png` 는 `192×192` = 32px 프레임 6×6이고
**실제로 쓰는 건 24프레임**이다 (idle 2 × 3방향 + 걷기 6 × 3방향, 왼쪽은 좌우반전).

세 캐릭터가 **같은 베이스 리그**를 쓰고 부위가 색으로 완전히 분리된다. 24프레임 전부에서 확인했다.

| character1 부위 | 색 | 프레임 |
|---|---|---|
| 헤어 | `#625565` `#7f708a` `#3e3546` | 24/24 |
| 상의 | `#fb6b1d` `#b33831` `#6e2727` | 24/24 |
| 재킷 | `#0eaf9b` `#0b8a8f` | 24/24 |
| 하의 | `#4d9be6` `#4d65b4` | 24/24 |
| 신발 | `#753c54` `#45293f` | 24/24 |
| 아웃라인 | `#2e222f` (전 부위 공유) | – |

character3(팀장)의 넥타이(`#f9c22b`)만 16/24 — 뒤·위 방향에서 안 보이는 게 정상이다.

### 기법 A — 팔레트 스왑 (파일 0장)

기존 옷의 픽셀 마스크를 스텐실로 쓰면 24프레임 정렬이 자동이다.
상의·하의·신발 각 8색이면 512조합. 고어룩(전신 검정)과 정장(네이비+흰셔츠)은 이것만으로 나온다.

### 기법 B — 모자 (방향별 3장 도트 정의, 파일 0장)

머리 바운딩박스가 전 프레임 고정이다.

```
아래·위  x9-22 (14px)      측면  x10-22 (13px)      y 시작만 2~4로 흔들림(머리 흔들림)
```

그래서 모자를 **아래·측면·위 3장만 문자열 도트로 정의**하고 헤어 박스 기준으로 얹으면
걷기 프레임까지 다 맞는다. 코드 안 문자열이라 **PNG가 아니다.**

```js
'down': ["...########...",      // # 아웃라인 / H 밝은면 / M 중간 / D 어두운면
         "..#HHHHHHHH#..",
         "#MMMMMMMMMMMM#", ...]
```

> **실패한 접근 기록** — 처음엔 헤어 마스크의 *컬럼별 최상단*을 따라 모자를 씌웠다.
> 머리카락 삐죽한 모양을 그대로 따라가서 모자가 울퉁불퉁해졌다.
> **모자는 머리를 따라가는 게 아니라 덮는 것이다.** 바운딩박스 기준으로 바꿔야 한다.

### 못 하는 것

| | 가능 |
|---|---|
| 모자 실루엣 변화 (비니 vs 캡) | ✅ 머리 위가 빈 공간이라 자유롭다 |
| 상의·하의 **색·패턴** | ✅ |
| 상의·하의 **실루엣** (후드티, 배기팬츠) | ❌ 벗은 베이스가 없고 아웃라인을 옷과 공유한다. 팔 위치가 프레임마다 달라 실루엣 확장은 품질이 무너진다 |

### 32px 톱다운에서 안 보이는 슬롯

| 슬롯 | 원본 픽셀 | 판정 |
|---|---|---|
| 모자 · 헤어 · 상의 · 하의 | 8~12px | ✅ 보인다 |
| 신발 | 4×3 | △ 색 구분 정도 |
| 눈 종류 · 눈동자 색 | **1~2px** | ❌ 얼굴이 거의 안 보인다 |
| 손목 악세서리 · 목걸이 | **1~2px** | ❌ 팔이 몸통에 붙고 상의에 가려진다 |

**표시면을 둘로 쪼개는 것이 답이다.**

| | 월드 스프라이트 | 초상화(아바타) |
|---|---|---|
| 비용 | 24프레임 | **1프레임** |
| 담당 | 실루엣 — 모자·상의·하의·신발 | 전부 — 눈, 눈동자, 목걸이, 시계까지 |
| 쓰는 곳 | 게임 화면 | 프로필·장비창·순위표 |

### 렌더링 — 반드시 캐싱

레이어를 매 프레임 겹쳐 그리면 안 된다 (화면에 캐릭터 최대 8명 × 레이어 수).
**레벨 시작에 한 번** 오프스크린 캔버스로 합성 시트를 구워 `IMG[key]` 자리에 꽂으면
`drawChar()` 는 지금처럼 `drawImage` 한 번으로 끝난다. 렌더 코드는 안 바뀐다.

---

## 2. 슬롯

```
보이는 슬롯    모자 · 상의 · 하의 · 신발          팔레트 + 도트
안 보이는 슬롯  워치 · 노트북 · 태블릿 · 목걸이 · 이어폰   애셋 0장, 데이터만
```

여러 개 동시 착용, **같은 슬롯은 하나만**. 별도 로직 없이 슬롯 시스템 그대로다.

---

## 3. 언어 친화 매트릭스

직군·언어 팩([`ARCHITECTURE.md`](ARCHITECTURE.md) 「직군 · 언어 팩」)에 얹힌다. 아이템에 `boost` 필드 하나.

| 아이템 | 슬롯 | JS | Kotlin | Swift | 기획자 |
|---|---|---|---|---|---|
| 애플워치 | 워치 | 소 | – | **대** | – |
| 삼성워치 | 워치 | 소 | **대** | – | – |
| 가민워치 | 워치 | 소 | 소 | 소 | 소 |
| 맥북 | 노트북 | 중 | – | **대** | 소 |
| 씽크패드 | 노트북 | 중 | **대** | – | – |
| 그램 | 노트북 | 소 | 소 | – | **대** |
| 아이패드 | 태블릿 | – | – | 중 | **대** |
| 갤탭 | 태블릿 | – | 중 | – | 중 |

> ⚠️ 그냥 놔두면 **JS가 양쪽 생태계에서 다 이득을 봐 최강 팩이 된다.**
> 해법이 현실과 맞는다 — JS는 **넓지만 얕게**(어디서나 되지만 전용 이득 없음),
> Kotlin·Swift는 **좁지만 깊게**. 위 표의 소/중/대가 그 배분이다.

---

## 4. 아이템 표

| 아이템 | 가격 | 이득 | 대가 |
|---|---|---|---|
| 🪪 스마트 사원증 | 30 | 남은 미션 지점에 화살표·아이콘 | – |
| 🏃 런닝화 | 120 | 이동속도 +8% (168→181px/s) | 살금살금 은폐 −45% (`.55`→`.78`) |
| 🥤 텀블러 | 150 | ☕커피 **운반** 삭제 (위험 동선 제거) | 설거지 요구량 +50% |
| 🎧 노이즈캔슬링 | 200 | 스몰토크 면역 | 들킴 게이지 +25% |
| 💼 사직서 | ~~200~~ → **20** ✅ | 야근 확정 1회 방어 → 그 날 재시작 | 그 레벨 클리어·시간 보너스 0 · 소모품(상한 5) |
| 🧥 롱코트 | 400 | ~~사직서 **2장**까지 소지~~ (상의 슬롯) — ⚠️ `STOCK_CAP 5` 가 들어와 이 이득은 무의미해졌다. 다른 이득을 붙일 것 | 상의 슬롯을 먹어 고어룩·정장 세트와 경쟁 |
| ⌨️ 기계식 키보드 | 250 | ⌨️커밋 par +30% | **타이핑 중에도 들킴 게이지가 찬다** |
| 🕴️ 정장 세트 | 400 | 임원 시야 −35%, 범위 −20% | 동료 슬랙 +50% · **LV5부터 효과** |
| ⌚🖥️📱 워치·노트북·태블릿 | 100~600 | 언어별 친화 | – |
| 🤖 Claude Code | ~~2,000~~ → **10** ✅ | 타이핑 미션 중 `Tab` → 즉시 수정·전송 | 소모품(상한 5) · 💬슬랙·🧽설거지는 제외 |

**기계식 키보드가 가장 좋은 설계다.** 지금 코드는 미션 중 들킴 게이지를 *동결*한다
(이동이 막혀 회피할 수 없으므로 — `ARCHITECTURE.md` 「미션 4종」).
이 아이템만 그 규칙을 깨서 **안전지대였던 타이핑이 위험해진다.**

### 세트

| 세트 | 조건 | 칭호 | 효과 |
|---|---|---|---|
| 애플 생태계 | 애플워치+맥북+아이패드 | 개발자의 가호 | Swift에서 🚨 6초 힌트 **즉시** |
| 안드로이드 진영 | 삼성워치+씽크패드+갤탭 | 안드로이드의 가호 | Kotlin에서 동일 |
| 고어룩 | 모자·상의·하의·신발 전부 검정 | 아웃사이더 | 스몰토크 확률 회피 |
| 정장 | 정장 상하 + 구두 | 임원 위장 | 위 표 참고 |

---

## 5. 계산해서 알아낸 함정

코드 상수를 넣어 뽑은 것들이다. **새 아이템을 만들 때 같은 계산을 먼저 할 것.**

### ❌ 엘베 문 열리는 시간은 이미 공짜다

```js
if (phase === 'run'){ timeLeft -= dt; ... }
else if (phase === 'open'){ elevator.open += dt*3.2; if (elevator.open >= 1) win(); }
```

문이 열리기 시작하면 `phase`가 `'open'`이 되고 **`timeLeft`가 멈춘다.**
`scoreBreak()`의 `timeB`도 그 정지된 값을 쓴다. 즉 0.31초의 문 열림은 무료다.

**"엘베를 빨리 열어주는 아이템"은 점수 영향이 정확히 0이다.** 도착 판정(`player.x > 812`)을
32px 앞당기는 쪽도 168px/s 기준 0.19초 = **+19점**이라 무의미하다.
→ 그래서 저가 아이템은 **점수가 아니라 정보**를 팔아야 한다 (스마트 사원증).

### ❌ 미션을 삭제하면 점수가 깎인다

`taskB = tasks.length * 200`. 미션을 없애는 아이템은 **−200점**을 동반한다.
텀블러를 "커피 미션 삭제"로 설계했다가 이 때문에 **"운반만 삭제"로 바꿨다.**

```js
coffee: {kind:'carry', hold:.5, at:'cup', to:'trash', hold2:.7}
        → 텀블러 착용 시  kind:'hold'      // 미션 수 유지, 왕복만 사라짐
```

### ❌ `DISH_GAIN`을 건드리면 난이도가 비선형으로 터진다

```js
const DISH_GAIN = 7, DISH_DECAY = 12;    // 누를 때 +7%, 가만히 있으면 초당 -12%
```

임계 연타수 = `DECAY / GAIN`. `DECAY`가 고정이라 `GAIN`을 줄이면 임계점이 움직인다.

| | 임계 연타 | 초당 3회 연타 시 |
|---|---|---|
| 기본 (GAIN 7) | 1.71회/초 | 11.1초 |
| GAIN 4.7 로 하향 | **2.55회/초** | **47.6초** ← 폭발 |

**설거지 양을 늘리려면 `GAIN`이 아니라 요구 진행량을 늘려야 한다.**
`prog`가 `Math.min(100, ...)`으로 상한이 박혀 있으니 `dish.need` 변수로 빼고
바 너비를 `prog/need*100`으로. `need 150`이면 초당 3회에 16.7초(+5.6초), 임계점은 1.71 유지.

### ⚠️ 슬랙 발생률 증가는 순수 페널티가 아니다

슬랙 답장은 ⚡보너스를 최대 450 주고, 답장하는 동안 얼어붙어 남은시간(×100)을 태운다.
답장 `내일 오전에 드릴게요` 11자 → `par = 1 + 11×0.36 = 4.96초`

| 답장 속도 | ⚡보너스 | 시간 손실 | 순손익 |
|---|---|---|---|
| 1.5초 | +315 | −150 | **+165** |
| 2.5초 | +250 | −250 | ±0 |
| 3.0초 | +178 | −300 | −122 |
| 5.0초 | 0 | −500 | −500 |

**손익분기 2.5초.** 정장은 "타이핑 빠른 사람용 하이리스크 빌드"가 된다.
숫자를 만질 때 기준은 **손익분기를 중위 플레이어 근처에 두는 것**이다.

### ⚠️ 🤖 Claude 는 점수로는 손익분기다

```
즉시 수정   ⚡ par 6.2초 중 0.3초 → ratio .95 → +665   +   시간 절약 6초 → +600   ≈ +1,000
버그 추가   이동 3~8초 + 고치기 5~8초                                        ≈ −1,000 ~ −1,500
```

**그래서 이 아이템의 가치는 점수가 아니라 분산 제거다.** LV25쯤 마감 34초에서
🚨 미션을 한 번 버벅이면 야근 확정이다. Claude는 불확실한 8초를 확정 0.3초로 바꾼다.

> ⚠️ **가격 논거는 뒤집혔다 (2026-08-28). 2,000 → 300 → 10.**
> 원래 논거는 «초보는 살 이유가 없고 벽에 부딪힌 사람만 산다 — 그래서 비싸도 된다» 였다.
> 틀린 부분은 **소모품이라는 사실을 값에 안 넣은 것**이다. 300코인은 LV6 여섯 판(실측
> 「코인과 상점」)을 모아서 `Tab` 한 번인데, 위 표대로 점수로는 손익분기다.
> 최상급 영구 의상(컬리 퍼플 300)과 같은 값이라 **아무도 안 산다 = 없는 아이템**이었다.
>
> 대신 **가격이 아니라 재고 상한(`STOCK_CAP = 5`)이 상한 레버**가 됐다. 코인은 영구 누적이라
> 가격으로는 결국 못 막는다 — 100판 한 사람은 어떤 값이든 산다. 값으로 막으면
> 초보만 막히고 고수는 안 막히는데, 개수로 막으면 반대로 **둘 다 같은 5개**다.
> 이쪽이 「벽에 부딪힌 사람의 응급 버튼」이라는 원래 의도에 실제로 더 가깝다.

`💬슬랙`과 `🧽설거지`는 **반드시 제외**해야 한다. 셋 다 되면 게임 전체를 대신해버린다.
명분도 있다 — 팀장 답장을 AI로 보내면 눈치채고, Claude는 설거지를 못 한다.
단축키는 `Tab`. `Enter`는 전송, `Escape`는 취소로 이미 쓰고 있고 슬랙 제한시간 4.5초 안에 두 키는 무리다.

### 💼 사직서 — 이름은 반드시 **물건**이어야 한다

처음엔 「🗓️ 연차」 + 「🏖️ 워라밸 세트」로 잡았다가 버렸다.
**연차는 결재 서류고 워라밸은 개념이라 장비창에 놓을 물건이 없다.**
메커니즘은 이름과 무관하므로 물건으로 바꿨다 — "가슴속에 사직서 한 장".

상한을 올리는 쪽도 개념(세트 이름)이 아니라 **담는 물건**이어야 한다.
🧥 롱코트는 보이는 슬롯이라 장비창에서 실제로 보이고, 상의 슬롯을 먹으니
고어룩·정장 세트와 경쟁한다 — 트레이드오프가 저절로 생긴다.

대안 라벨: 💊 박카스(스택이 자연스럽다) · 🔑 비상계단 마스터키(재시도가 아니라 다른 탈출 = 메커니즘이 다르다)

### ⚠️ 사직서의 실질 레버는 페널티가 아니라 사용 횟수다

`lose()`는 점수를 더하지 않는다(`score += br.total`은 `win()`에만 있다).
실패한 레벨은 원래 0점이라 재시도해도 계산이 안 꼬인다. 구현은 `R` 키 경로 그대로다.

```js
if (c === 'KeyR' && state === 'playing') startWave(wave);   // 시드 유지 = 같은 배치
if (timeLeft <= 0){ phase = 'closed'; lose(); }             // ← 사직서 있으면 startWave(wave)
```

"시간 보너스만 0" 페널티는 **정작 필요한 고레벨에서 거의 무의미하다.**

| | 남은 시간 | timeB 페널티 |
|---|---|---|
| LV10 (마감 94초) | 50초+ | −5,000 (여기선 사직서를 안 쓴다) |
| LV25 (마감 34초) | 2~3초 | **−300** |

그래서 클리어 보너스까지 함께 0으로 한다 (LV25 기준 17,200 → 4,400).
다만 진짜 가치는 그 레벨 점수가 아니라 **런이 안 끊기는 것**이다 — 이후 레벨이 만점으로 붙는다.
따라서 밸런스 레버는 **한 런에 몇 장 쓸 수 있는지**이고, 소모품으로 두어야 한다.

사직서 1장이 주는 추가 점수는 벽에서 대략 +5,000~15,000점 = **5~15코인**이다.
가격 20이면 **코인 효율은 손해~손익분기**다 — 파밍 전략이 안 되므로 그게 건강하다.
(원래 200이었다. 위 Claude 와 같은 이유로 내렸다.)

**🤖 Claude(10) 보다 💼 사직서(20) 가 비싼 것은 의도된 순서다.** 개당 가치가 사직서가 위다 —
야근 확정은 **런 자체가 끝나는 것**이고 사직서는 이후 레벨 전부를 살리는데,
Claude 가 지키는 건 미션 하나다. 구현 전에는 300 > 200 으로 거꾸로 붙어 있었다.

재도전은 **같은 시드 = 같은 배치**다(`startWave`가 `runSeed`를 유지). 두 번째 시도는 확실히 쉽다.
시드를 새로 뽑으면 「같은 시드는 같은 배치」 규칙을 깨야 하니 그냥 유리하게 두는 게 낫다.

### ✅ 시드 규격은 문제없다

아이템이 슬랙 빈도나 버그 개수를 바꾸면 `rEvt` 소비 횟수가 달라진다.
**하지만 `rEvt`는 이미 플레이 방식에 따라 갈린다** — 미션을 시작하는 시점에 버그·커밋을 뽑기 때문이다.
맵 배치를 결정하는 `rMap`은 레벨 시작에 한 번만 소비되므로 **같은 시드는 어느 장비에서든 같은 배치**다.
데일리 챌린지 전제는 유지된다. (`CLAUDE.md` 「깨뜨리면 안 되는 것」 4번)

---

## 6. 순위표 — 숨기지 말 것

아이템 효과가 시간·점수를 직접 올리면 순위표가 **장비 순위표**가 된다.

| 방식 | 비용 |
|---|---|
| **효과를 정보형으로만** 설계 | 순위표 안 건드림. 권장 |
| 시간·점수 가산 허용 | `pack_scores`에 `bare` 컬럼 + 「효과 없는 판만」 토글 |

💼사직서는 어느 쪽이든 **사용 횟수를 점수와 함께 제출**해서 순위표에 `💼2` 배지로 드러낼 것.
`seed`를 이미 보내고 있으니 컬럼 하나다. 숨기면 나중에 반드시 시끄러워진다.

---

## 7. 화폐

```
코인 = 런 최종 점수 / 1,000        (LV12 100,000점 = 100코인)
```

| 등급 | 예 | 가격 | 체감 |
|---|---|---|---|
| **소모품** | 🤖 Claude Code, 💼 사직서 | **10~20** | 한 판에 몇 장 · **상한 5** |
| 저렴 | 스마트 사원증, 에어팟 | 30~60 | 한두 판 |
| 보통 | 워치류, 모자, 텀블러 | 100~250 | 두세 판 |
| 고급 | 맥북, 씽크패드, 정장, 롱코트 | 400~600 | 대여섯 판 |

**소모품은 별도 등급이다.** 한 번 쓰면 사라지므로 최저가 영구템(의상 40)보다 싸야 하고,
누적을 막는 것은 값이 아니라 개수다. 위 「🤖 Claude」 절의 경고를 읽을 것.

인벤토리는 pid 기준으로 D1 테이블 하나. 점수가 이미 클라이언트 신뢰 모델이므로
인벤토리도 같은 수준으로 두면 된다 (`ARCHITECTURE.md` 「알려진 한계」).

---

## 8. 구현 순서

1. ~~**팔레트 스왑 + 장비창 UI + 의상 아이템**~~ — **✅ 구현됨.**
   헤더 `👕 장비` → `👔 의상`(10종 카드) / `🎨 직접 배색`(5슬롯 × 13색).
   **의상이 곧 아이템 단위**이므로 상점과 세트 효과가 이 단위에 그대로 붙는다.
   구현 내용은 [`ARCHITECTURE.md`](ARCHITECTURE.md) 「캐릭터 외형」.
2. ~~**안 보이는 슬롯 + 효과**~~ — **✅ 구현됨.** ⌚워치·💻노트북·📱태블릿 3슬롯 8종 +
   소비템 2종(🤖 Claude Code, 💼 사직서). 언어 친화 매트릭스가 par 배율로 살아났다.
   구현 내용은 [`ARCHITECTURE.md`](ARCHITECTURE.md) 「장비템」·「소비템」.
   **아직 안 한 것** — 의상 세트 효과(정장 → 임원 위장, 고어룩 → 아웃사이더),
   기계식 키보드·텀블러·런닝화 같은 트레이드오프 아이템,
   Claude 의 '버그 추가' 대가(런 중 체크리스트 동적 추가가 필요하다. 아래 참고).
3. **모자** — 도트 정의. 비니·캡·버킷햇·안전모면 실루엣이 충분히 갈린다.
   **보류 (2026-08-24).** 기법은 검증됐지만 손으로 그린 도트 품질이 게임에 넣을 수준이 안 된다.
   제대로 된 레이어드 애셋이나 픽셀 아티스트가 붙을 때 재개할 것 —
   `buildLook()` 합성 경로에 그대로 얹으면 되고, 도트 정의 형식과 실패한 접근은 위 1장에 있다.
4. **초상화** — 목걸이·시계를 눈으로 보고 싶을 때. 아이템당 1장

**코인 + 상점도 ✅ 구현됐다.** 가격표와 벌이 실측은
[`ARCHITECTURE.md`](ARCHITECTURE.md) 「코인과 상점」.

⚠️ **효과 있는 아이템을 더 붙일 때는 이 문서의 「계산해서 알아낸 함정」을 먼저 읽을 것.**
엘베 문 열리는 시간은 이미 공짜라 점수 영향이 0이고, 미션을 삭제하면 `taskB` 때문에 −200점이며,
`DISH_GAIN` 을 건드리면 난이도가 비선형으로 폭발한다.

1~2단계가 애셋 없이 굴러가고 재미의 대부분이 거기 있다.

### 런 중 체크리스트 추가 (Claude 아이템 전제)

지금 `tasks`는 레벨 시작에 확정되고 런 중 안 바뀐다. Claude의 "버그 추가"가 **첫 사례**다.

| | 지금 | 필요 |
|---|---|---|
| 오류 자리 | `POINTS.err` 1개 | 2개 이상 (`err2`) |
| 체크리스트 | 레벨 시작 고정 | 런 중 추가 + `htmlCache.todo` 무효화 |
| 엘베 조건 | `tasks.every(done)` | 그대로 — 자동 연장된다 |

버그가 생기는 자리를 **이미 끝낸 책상 중에서** `rEvt`로 뽑으면 되돌아가야 해서 동선 비용이 확실해진다.
