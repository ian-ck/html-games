# 설계 문서 — 퇴근 스쿨버스

게임 규칙은 [`README.md`](README.md), 작업 규약은 [`CLAUDE.md`](CLAUDE.md).
이 문서는 **코드를 읽어서는 바로 안 보이는 결정과 수치**를 담는다.

## 좌표계

캔버스 `960 × 608` = **30 × 19 타일**, 타일 하나 `32px`.

원본 스프라이트는 16px 기준이라 **항상 2배로 그린다.**

| 원본 | 화면 | 예 |
|---|---|---|
| 16×16 | 32×32 (1타일) | 의자, 쓰레기통, 싱크대 |
| 32×32 | 64×64 (2×2타일) | 책상, 자판기, 화분, 변기 |
| 32×32 프레임(캐릭터) | 64×64 | 사람 (실제 몸통은 ~32×56) |

캐릭터 시트는 `192×192` = 32px 프레임 6×6.
`r0` idle하 · `r1` 걷기하 · `r2` idle측 · `r3` 걷기측 · `r4` idle위 · `r5` 걷기위.
**측면은 오른쪽을 보고 있고 왼쪽은 좌우반전**해서 쓴다.
그리기: `drawImage(sheet, f*32, row*32, 32,32, x-32, y-56, 64,64)` — 발이 `y`에 오도록 위로 56 올린다.

## 주요 좌표

```
외벽            두께 32
엘베 홀 분리벽   x768~800  (통로: y256~352 만 열림)
엘리베이터       x864~928, y256~352
화장실          x800~928, y32~152   (입구는 왼쪽 아래)
책상 12개       x∈{96,256,416} × y∈{64,192,320,448}, 각 64×64
팀장 자리       (640,256) + 의자 (640,318)
탕비실          정수기(480,32) 커피머신(544,32) 싱크대(544,64) 자판기(592,32)
쓰레기통        (672,480)   프린터(480,448)
엄폐물          Big-Plant(608,96) · Tall-Bookshelf(512,288)
```

**미션 지점은 책상 앞(아래) 통로** = `(desk.x+32, desk.y+80)`. 네 줄 모두 아래에 64px 통로가 있어 안전하다.

## 랜덤 배치

레벨마다 `rMap` 스트림에서 뽑는다. **같은 시드 = 같은 배치.**

| 대상 | 후보 | 표시 |
|---|---|---|
| 내 자리 | 책상 12곳 | 🟢 초록 모니터 |
| 오류 발생 | 책상 12곳 (내 자리 제외) | 🔴 빨간 모니터 + 맥동하는 경고등 |
| 커피 | 책상 12곳 (위 둘 제외) | ☕ 책상 위 컵 + 떠 있는 컵 아이콘 |
| 사물함 | **모서리 3곳 고정** (좌상·좌하·우하) | 🎒 떠 있는 가방 아이콘 |

사물함만 프리셋인 이유: 완전 랜덤이면 맵 구성이 흐트러진다.
나머지 책상은 12곳 어디든 통로 폭이 같아 문제없다.

`Desk-Clean`(컵 없는 책상)이 기본이고, **컵은 커피 자리 하나에만** 그려진다.
원본 `Desk.png`에 머그가 그려져 있어 12개 전부 컵을 든 것처럼 보이던 문제를 이렇게 해결했다.

## 시드 난수

```js
rMap  // 맵 배치. 레벨 시작에 한 번만 소비 → 플레이 방식과 무관하게 재현됨
rEvt  // 슬랙/버그/커밋 문구, 회의 소집 타이밍
```

런(run) 단위로 `runSeed`를 잡고 레벨별로 `hashSeed(runSeed + ':map:' + lv)` 로 파생한다.
`R` 재시작은 시드를 유지(같은 판 재도전), 새 판은 새 시드.
`?seed=abc` 로 고정 가능.

**이 분리가 데일리 챌린지와 멀티플레이의 전제조건이다.** 거품·효과음은 시드를 쓰지 않는다.

## 레벨 진행

할 일은 `INTRO` 순서로 하나씩 늘어난다.

| LV | 추가되는 할 일 | 할 일 수 | 마감 | 팀장 | 동료 | 임원 | 시선 패턴(변곡점) |
|---|---|---|---|---|---|---|---|
| 1 | 💻노트북 · 🎒가방 | 2 | 42초 | 1 | 0 | – | 좌우 왕복 (2) |
| 2 | ⌨️커밋 + 💬슬랙 | 3 | 53초 | 1 | 0 | – | 좌우 왕복 (2) |
| 3 | ☕커피 | 4 | 66초 | 1 | 1 | – | 되돌아보기 (4) |
| 4 | 🚻화장실 | 5 | 78초 | 1 | 2 | – | 되돌아보기 (4) |
| 5 | 🚨오류수정 | 6 | 92초 | 1 | 2 | 1 | 3단 스캔 (6) |
| 6 | 🧽설거지 | 7 | 106초 | 2 | 3 | 1 | 3단 스캔 (6) |
| 7 | 📣회의 소집 | 7 | 103초 | 2 | 4 | 1 | 불규칙 (8) |
| 9 | – | 7 | 97초 | 2 | 5 | 1 | 불규칙 (8) |
| 30+ | – | 7 | **34초 (하한)** | 2 | 5 | 1 | 불규칙 (8) |

`deadline = max(34, BASE[할일수] - max(0, LV-6) × 3)`

**LV13 이후로는 마감 시간만 줄어든다.** 다른 파라미터는 이미 한계에 도달한다
(할 일 LV6, 팀장 LV6, 시선 LV7, 동료 LV9, 멈춤/회전 시간 LV12~13).
LV30에서 완전히 정지하며 그 뒤로는 동일한 판이 반복된다.

실측 최소 소요는 31~37초라 **실질 벽은 LV25~28**이다. 여기가 자연스러운 실력 천장.

### 팀장 시선

플레이어를 **절대 쫓지 않는다.** 키앵글 배열을 순서대로 훑을 뿐이고 시간의 함수다.
"멈춤 → 회전 → 멈춤 → …"이라 리듬을 읽어 통과하는 게 핵심 재미.

```js
GAZE = [[-1,1], [-1,1,-.3,1], [-1,0,1,0,-1,.55], [-1,1,-1,.4,-.45,1,0,-.75]]
hold = max(.5,  1.45 - LV*.08)     // 멈춤 1.37초 → 0.50초
turn = max(.42, 1.05 - LV*.05)     // 회전 1.05초 → 0.42초
```

팀장이 2명일 땐 두 번째가 **키앵글을 뒤집고 위상도 2.7초 어긋나게** 해서 동시에 움직이지 않는다.

## 점수

```
클리어 보너스     LV × 500
남은 시간         남은초 × 100        ← 전체의 60~70%. 사실상 타임어택 게임
할 일 개수        개수 × 200
⚡ 속도 보너스     타이핑·설거지 (아래)
🤫 무적발         +1,000 (한 번도 안 들켰을 때)
```

속도 보너스는 `par` 대비 얼마나 빨랐는지로 계산한다.

| 종류 | par | 최대 |
|---|---|---|
| 슬랙 답장 | `1.0 + 글자수 × 0.36` (전 팩 공통 — 한글) | 450 |
| 커밋 / 스펙 확정 | `1.0 + 글자수 × P.termK` | 600 |
| 오류 / 정책 수정 | `P.fixRead + 글자수 × 0.12` (읽고 찾는 시간 포함) | 700 |
| 설거지 | `9.0` 고정 | 500 |

`P.termK` · `P.fixRead` 는 직군·언어 팩이 정한다 (아래 「직군 · 언어 팩」).

점수는 레벨을 넘겨도 누적된다. 야근(마감 초과) 시 런 종료.

## 미션 4종

| 미션 | 방식 |
|---|---|
| 홀드형 (노트북·가방·화장실·커피) | 지점에서 가만히 서 있으면 게이지 |
| ⌨️ 커밋 / 💬 슬랙 | 제시 문장을 **그대로** 입력 후 **Enter** |
| 🚨 오류 수정 | 버그 코드가 **미리 채워진** 입력창에서 커서로 오타만 고침. 정답은 안 보여주고 콘솔 에러만 준다. 6초 후 힌트 |
| 🧽 설거지 | 모달 미니게임. **Enter 연타** (+7%/회, -12%/초). 초당 1.7회 이하면 영원히 안 끝남 |

- 코드 입력(커밋·오류수정)은 **ASCII만 허용**하고 한글은 자동으로 걸러낸다(IME 안내 표시).
- Enter 자동반복 방지: 설거지는 **키를 떼야** 다음 입력이 인정된다.
- **미션 중에는 방해 효과를 전부 무시한다** — 이동이 막혀 회피할 수 없기 때문.
  들킴 게이지는 0으로 리셋하지 않고 **그 자리에 동결**한다(미션을 안전지대 세탁에 쓰지 못하게).
  단 슬랙 시간 초과 페널티는 유지 — 그건 회피 가능한 실패다.
- 미션 중에는 슬랙이 오지 않는다(모달 중첩 방지).

## 직군 · 언어 팩

시작 화면에서 고른다. **⌨️커밋 / 🚨오류수정의 내용만** 바뀌고 맵·마감시간·NPC·시드는 전부 공통이다.
같은 시드는 어느 팩에서든 같은 배치를 낸다 — 팩 키는 시드 파생식에 들어가지 않는다.

| 팩 키 | 직군 | 언어 | ascii | termK | fixRead |
|---|---|---|---|---|---|
| `dev:js` | 개발자 | JavaScript | ✅ | .30 | 3.5 |
| `dev:kotlin` | 개발자 | Kotlin | ✅ | .30 | 3.5 |
| `dev:swift` | 개발자 | Swift | ✅ | .30 | 3.5 |
| `pm:policy` | 기획자 | 정책 | ❌ | **.62** | **4.0** |

`localStorage['lot.pack']` 에 남고 `?pack=pm:policy` 로 덮어쓸 수 있다.

### 두 미션의 축을 분리해야 하는 이유

```
term(커밋)  정답을 보여준다   → 손 속도 승부
fix(오류)   정답을 숨기고 증상만 → 위치 판단 승부. 고칠 곳은 항상 한 군데
```

개발자판 `BUGS` 의 `err` 는 **틀린 토큰을 거의 그대로 알려준다**(12개 중 11개).
즉 fix 의 실제 난이도는 퍼즐이 아니라 *빨간 모니터까지 가는 동선 + 커서 이동*이다.

기획자판을 만들 때 이 친절도를 유지하지 않으면 난이도가 양쪽으로 튄다.
증상이 위치를 알려주면 타이핑량이 적어 **fix 가 term 보다 쉬워지고**,
증상을 모호하게 하면 par(`fixRead + 글자×.12`) 안에 못 끝내 **⚡보너스가 통째로 0**이 된다.
그래서 기획자판 증상도 "무엇이 잘못됐는지"까지 말해준다 — 콘솔 에러와 같은 친절도로.

기획자판 term 을 '문의 대응'으로 하면 **💬슬랙과 겹쳐 미션 세 개가 하나로 붕괴한다.**
그래서 term 자리는 문의 대응이 아니라 **확정·선언**(스펙 확정, 릴리즈 노트)이다.
서사적으로도 커밋은 "내 일을 확정해 남에게 넘기는 도장"이고 노트북 끄기가 여기 잠겨 있다.

### 팩 규격 — 어기면 조용히 깨진다

1. `bugs` 정확히 **12개**, `commits` 정확히 **6개**. `rEvt()` 로 인덱싱하므로 길이가 다르면
   같은 시드가 팩마다 다른 문제를 낸다 → 데일리 챌린지·재현성 전제가 무너진다.
2. **같은 슬롯 번호는 팩끼리 같은 난이도.** 팩 주석의 `SLOTS` 목록이 그 계약이다.
   (0 반복문키워드 · 1 조건키워드 · 2 return · 3 로그함수 · 4 while · 5 import ·
   6 변수선언 · 7 else if · 8 닫는괄호 · 9 대소문자 · 10 프로퍼티이름 · 11 비교연산자)
3. 글자수를 **15~40자 밴드** 안에. par 이 글자수의 함수라 밴드를 벗어나면 팩끼리 점수가 어긋난다.
4. `ascii:true` 팩은 본문에 **한글을 쓸 수 없다** — 입력창이 한글을 걸러낸다.

실측 캘리브레이션 (평균 par):

| 팩 | fix | term | LV5 실측 typeSum |
|---|---|---|---|
| dev:js | 6.17s | 9.05s | — |
| dev:kotlin | 5.96s | 8.90s | 1,051 |
| dev:swift | 5.58s | 9.00s | 1,019 |
| pm:policy | 6.54s | 7.67s | 1,049 |

한글은 한 글자에 2~3 타 + 한/영 전환이 있어 `termK` 를 ASCII 의 두 배(.62)로 잡았다.
`fixRead` 4.0 은 정책문 한 줄 읽는 시간이다.

## 신원 모델

```
pid   숨은 고유 ID. D1 기본키. 신규는 16자 랜덤 hex
nick  표시용 라벨. 언제든 바꿀 수 있다 (2~10자 + #16진수3자)
꼬리  pid 앞 3자리에서 파생 — 이름을 바꾸거나 🎲를 돌려도 유지되는 식별 배지
```

**닉네임을 키로 쓰면 이름을 바꿀 때마다 새 행이 생겨 같은 사람이 중복된다.** 그래서 분리했다.

pid 도입 전 사용자는 `닉네임 전체의 UTF-8 hex`로 pid를 유도해 옛 기록에 이어붙는다
(SQLite `lower(hex(nick))`와 동일). **앞부분만 잘라 쓰면 안 된다** — 한글은 글자당 3바이트라
16자로는 2.7자밖에 안 담겨 앞 글자가 겹치는 닉네임끼리 충돌한다(실제로 한 명의 기록이 사라졌다).

## API (Cloudflare Worker + D1)

```
GET  /top?n=10&pid=...&role=&lang=  { rows:[{nick,lv,score}], myRank, total }
POST /score {pid,nick,lv,score,seed,role,lang}
GET  /posts?n=30&before=<id>      커서 페이지네이션 (무한스크롤)
POST /post  {pid,nick,body}
POST /post/del {pid,id}           pid 일치하는 글만 삭제
```

### 순위표 탭

`scores` 는 손대지 않았다 — **`pid` 당 한 행(역대 최고점) = 「전체」 탭**이다.
한 사람이 여러 팩을 뛸 수 있으므로 팩별 최고점은 별도 테이블에 남긴다.

```sql
pack_scores  PRIMARY KEY (pid, role, lang)     -- migrate-004-pack.sql
```

탭은 **전체 · JS · Kotlin · Swift · 기획자** 다섯 개다. 개발자 탭은 세 언어의 합집합이라 뺐다.
언어 탭은 `lang=`, 기획자 탭은 `role=pm` 으로 거른다 — 기획자에 도메인(커머스·물류·정산)이
늘어도 그 탭이 전부 모아주도록 `role` 쪽으로 잡았다.

`/top` 에 `role=` 또는 `lang=` 이 오면 `pack_scores` 를, 없으면 `scores` 를 본다.
`myRank` 도 같은 필터 안에서 센다.

**팩 도입 전 42행은 `migrate-004` 가 `dev:js` 로 백필한다.** 그 시절 커밋 문장도 버그 코드도
JavaScript 판 하나뿐이었으므로 추정이 아니라 사실이다. 클라이언트의 `readBoard()` 도
role 이 없는 로컬 행을 같은 기준으로 `dev:js` 로 읽는다 — **두 기준이 어긋나면 서버가
살아있을 때와 죽었을 때 다른 목록이 나온다.**

팩이 여러 개가 된 뒤로는 이 가정을 절대 쓸 수 없다. `role`/`lang` 이 빠진 제출은
(옛 클라이언트 캐시) 「전체」에만 들어간다.

`scores` 의 PK 를 `(pid, role, lang)` 으로 바꾸는 방법이 더 정규적이지만
살아있는 42행을 재생성해야 해서 택하지 않았다.

클라이언트의 `LBTABS` 는 서버 필터(`q`)와 로컬 폴백 필터(`f`)를 **한 줄에 나란히** 둔다.
한쪽만 고치면 서버가 살아있을 때와 죽었을 때 다른 목록이 나와 버그로 보이지 않는다.

서버 검증: 오리진 화이트리스트 · pid/닉네임 형식 · `role`/`lang` 화이트리스트 ·
`role`·`lang` 은 둘 다 있거나 둘 다 없어야 함(옛 클라이언트 호환) · 레벨 1~99 ·
점수 개연성(`20000×LV + 250×LV×(LV+1)` 초과 거부) · 레이트리밋(점수 5초, 글 10초, IP는 해시만 저장).

**클라이언트 조작을 원천 차단할 수는 없다.** 정적 사이트라 개연성 범위 안의 가짜 점수는 넣을 수 있다.
완전 차단은 시드 + 입력 로그를 보내 서버가 재시뮬레이션해야 하고, **시드 기반 난수는 그 준비가 되어 있다.**

클라이언트는 `LB` / `BOARD` 래퍼 뒤에 있어서 **`LB.url` 한 줄만 바꾸면 백엔드를 갈아끼울 수 있다.**
서버가 죽으면 자동으로 localStorage 기록으로 대체되어 게임이 멈추지 않는다.

## 다음에 할 만한 것

1. **데일리 챌린지** — 시드 기반 난수가 이미 있으므로 Worker가 그날의 시드를 내려주기만 하면 된다.
   실시간 없이도 "오늘 배치에서 누가 제일 빨리 나갔나" 경쟁이 성립한다. 가장 값싸고 효과가 크다.
2. **LV13+ 단조로움 해소** — 지금은 마감만 줄어든다.
   할 일 7개 중 5개를 시드로 뽑으면 같은 레벨도 매번 동선이 달라진다.
2-b. **팩 추가** — 디자이너(피그마 코멘트 반영 / 컴포넌트 네이밍 오류)나
   데이터 분석가(쿼리 오타 / 지표 정의 오류)는 위 규격만 지키면 콘텐츠 팩 하나로 끝난다.
3. **실시간 멀티플레이** — 같은 계정·프로젝트에 **Durable Objects** 바인딩만 추가하면 된다.
   방 하나 = 인스턴스 하나(싱글스레드)라 경쟁 조건이 없고, WebSocket을 직접 물릴 수 있다.
   마이그레이션 없이 확장 가능한 게 Cloudflare를 고른 이유다.
4. **화장실·사물함 외 지점에도 목표 아이콘** — 초반 위치 학습에 도움.

## 알려진 한계

- 순위표 점수는 클라이언트가 만든다 (위 참고).
- 닉네임 중복을 막지 않는다. `#꼬리`로 구분된다. 욕설 필터 없음.
- 게시판에 신고/관리자 기능이 없다. 본인 글 삭제만 가능.
- Worker 점수 검증이 `lv ≤ 99`다. 난이도를 크게 완화하면 상한에 걸릴 수 있다.
