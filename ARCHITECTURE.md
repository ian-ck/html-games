# 설계 문서 — 퇴근 스쿨버스

게임 규칙은 [`README.md`](README.md), 작업 규약은 [`CLAUDE.md`](CLAUDE.md).
이 문서는 **코드를 읽어서는 바로 안 보이는 결정과 수치**를 담는다.

## 좌표계

캔버스 `960 × 608` = **30 × 19 타일**, 타일 하나 `32px`.

원본 스프라이트는 16px 기준이라 **항상 2배로 그린다.**

| 원본 | 화면 | 예 |
|---|---|---|
| 16×16 | 32×32 (1타일) | 의자, 쓰레기통, 싱크대 |
| 32×32 | 64×64 (2×2타일) | 책상, 자판기, 화분, 변기 |
| 32×32 프레임(캐릭터) | 64×64 | 사람 (실제 몸통은 ~32×56) |

캐릭터 시트는 `192×192` = 32px 프레임 6×6.
`r0` idle하 · `r1` 걷기하 · `r2` idle측 · `r3` 걷기측 · `r4` idle위 · `r5` 걷기위.
**측면은 오른쪽을 보고 있고 왼쪽은 좌우반전**해서 쓴다.
그리기: `drawImage(sheet, f*32, row*32, 32,32, x-32, y-56, 64,64)` — 발이 `y`에 오도록 위로 56 올린다.

## 주요 좌표

```
외벽            두께 32
엘베 홀 분리벽   x768~800  (통로: y256~352 만 열림)
엘리베이터       x864~928, y256~352
화장실          x800~928, y32~152   (입구는 왼쪽 아래)
책상 12개       x∈{96,256,416} × y∈{64,192,320,448}, 각 64×64
팀장 자리       (640,256) + 의자 (640,318)
탕비실          정수기(480,32) 커피머신(544,32) 싱크대(544,64) 자판기(592,32)
쓰레기통        (672,480)   프린터(480,448)
엄폐물          Big-Plant(608,96) · Tall-Bookshelf(512,288)
```

**미션 지점은 책상 앞(아래) 통로** = `(desk.x+32, desk.y+80)`. 네 줄 모두 아래에 64px 통로가 있어 안전하다.

## 랜덤 배치

레벨마다 `rMap` 스트림에서 뽑는다. **같은 시드 = 같은 배치.**

| 대상 | 후보 | 표시 |
|---|---|---|
| 내 자리 | 책상 12곳 | 🟢 초록 모니터 |
| 오류 발생 | 책상 12곳 (내 자리 제외) | 🔴 빨간 모니터 + 맥동하는 경고등 |
| 커피 | 책상 12곳 (위 둘 제외) | ☕ 책상 위 컵 + 떠 있는 컵 아이콘 |
| 사물함 | **모서리 3곳 고정** (좌상·좌하·우하) | 🎒 떠 있는 가방 아이콘 |

사물함만 프리셋인 이유: 완전 랜덤이면 맵 구성이 흐트러진다.
나머지 책상은 12곳 어디든 통로 폭이 같아 문제없다.

`Desk-Clean`(컵 없는 책상)이 기본이고, **컵은 커피 자리 하나에만** 그려진다.
원본 `Desk.png`에 머그가 그려져 있어 12개 전부 컵을 든 것처럼 보이던 문제를 이렇게 해결했다.

## 시드 난수

```js
rMap  // 맵 배치. 레벨 시작에 한 번만 소비 → 플레이 방식과 무관하게 재현됨
rEvt  // 슬랙/버그/커밋 문구, 회의 소집 타이밍
```

런(run) 단위로 `runSeed`를 잡고 레벨별로 `hashSeed(runSeed + ':map:' + lv)` 로 파생한다.
`R` 재시작은 시드를 유지(같은 판 재도전), 새 판은 새 시드.
`?seed=abc` 로 고정 가능.

**이 분리가 데일리 챌린지와 멀티플레이의 전제조건이다.** 거품·효과음은 시드를 쓰지 않는다.

## 레벨 진행

할 일은 `INTRO` 순서로 하나씩 늘어난다.

| LV | 추가되는 할 일 | 할 일 수 | 마감 | 팀장 | 동료 | 임원 | 시선 패턴(변곡점) |
|---|---|---|---|---|---|---|---|
| 1 | 💻노트북 · 🎒가방 | 2 | 42초 | 1 | 0 | – | 좌우 왕복 (2) |
| 2 | ⌨️커밋 + 💬슬랙 | 3 | 53초 | 1 | 0 | – | 좌우 왕복 (2) |
| 3 | ☕커피 | 4 | 66초 | 1 | 1 | – | 되돌아보기 (4) |
| 4 | 🚻화장실 | 5 | 78초 | 1 | 2 | – | 되돌아보기 (4) |
| 5 | 🚨오류수정 | 6 | 92초 | 1 | 2 | 1 | 3단 스캔 (6) |
| 6 | 🧽설거지 | 7 | 106초 | 2 | 3 | 1 | 3단 스캔 (6) |
| 7 | 📣회의 소집 | 7 | 103초 | 2 | 4 | 1 | 불규칙 (8) |
| 9 | – | 7 | 97초 | 2 | 5 | 1 | 불규칙 (8) |
| 30+ | – | 7 | **34초 (하한)** | 2 | 5 | 1 | 불규칙 (8) |

`deadline = max(34, BASE[할일수] - max(0, LV-6) × 3)`

**LV13 이후로는 마감 시간만 줄어든다.** 다른 파라미터는 이미 한계에 도달한다
(할 일 LV6, 팀장 LV6, 시선 LV7, 동료 LV9, 멈춤/회전 시간 LV12~13).
LV30에서 완전히 정지하며 그 뒤로는 동일한 판이 반복된다.

실측 최소 소요는 31~37초라 **실질 벽은 LV25~28**이다. 여기가 자연스러운 실력 천장.

### 팀장 시선

플레이어를 **절대 쫓지 않는다.** 키앵글 배열을 순서대로 훑을 뿐이고 시간의 함수다.
"멈춤 → 회전 → 멈춤 → …"이라 리듬을 읽어 통과하는 게 핵심 재미.

```js
GAZE = [[-1,1], [-1,1,-.3,1], [-1,0,1,0,-1,.55], [-1,1,-1,.4,-.45,1,0,-.75]]
hold = max(.5,  1.45 - LV*.08)     // 멈춤 1.37초 → 0.50초
turn = max(.42, 1.05 - LV*.05)     // 회전 1.05초 → 0.42초
```

팀장이 2명일 땐 두 번째가 **키앵글을 뒤집고 위상도 2.7초 어긋나게** 해서 동시에 움직이지 않는다.

## 점수

```
클리어 보너스     LV × 500
남은 시간         남은초 × 100        ← 전체의 60~70%. 사실상 타임어택 게임
할 일 개수        개수 × 200
⚡ 속도 보너스     타이핑·설거지 (아래)
🤫 무적발         +1,000 (한 번도 안 들켰을 때)
```

속도 보너스는 `par` 대비 얼마나 빨랐는지로 계산한다.

| 종류 | par | 최대 |
|---|---|---|
| 슬랙 답장 | `1.0 + 글자수 × 0.36` (계수는 전 팩 공통 — 한글) | 450 |
| 커밋 / 스펙 확정 | `1.0 + 글자수 × P.termK` | 600 |
| 오류 / 정책 수정 | `P.fixRead + 글자수 × 0.12` (읽고 찾는 시간 포함) | 700 |
| 설거지 | `9.0` 고정 | 500 |

`P.termK` · `P.fixRead` 는 직군·언어 팩이 정한다 (아래 「직군 · 언어 팩」).

점수는 레벨을 넘겨도 누적된다. 야근(마감 초과) 시 런 종료.

## 미션 4종

| 미션 | 방식 |
|---|---|
| 홀드형 (노트북·가방·화장실·커피) | 지점에서 가만히 서 있으면 게이지 |
| ⌨️ 커밋 / 💬 슬랙 | 제시 문장을 **그대로** 입력 후 **Enter** |
| 🚨 오류 수정 | 버그 코드가 **미리 채워진** 입력창에서 커서로 오타만 고침. 정답은 안 보여주고 콘솔 에러만 준다. 6초 후 힌트 |
| 🧽 설거지 | 모달 미니게임. **Enter 연타** (+7%/회, -12%/초). 초당 1.7회 이하면 영원히 안 끝남 |

- 코드 입력(커밋·오류수정)은 **ASCII만 허용**하고 한글은 자동으로 걸러낸다(IME 안내 표시).
- Enter 자동반복 방지: 설거지는 **키를 떼야** 다음 입력이 인정된다.
- **미션 중에는 방해 효과를 전부 무시한다** — 이동이 막혀 회피할 수 없기 때문.
  들킴 게이지는 0으로 리셋하지 않고 **그 자리에 동결**한다(미션을 안전지대 세탁에 쓰지 못하게).
  단 슬랙 시간 초과 페널티는 유지 — 그건 회피 가능한 실패다.
- 미션 중에는 슬랙이 오지 않는다(모달 중첩 방지).

## 직군 · 언어 팩

시작 화면에서 고른다. **⌨️커밋 / 🚨오류수정의 내용만** 바뀌고 맵·마감시간·NPC·시드는 전부 공통이다.
같은 시드는 어느 팩에서든 같은 배치를 낸다 — 팩 키는 시드 파생식에 들어가지 않는다.

| 팩 키 | 직군 | 언어 | ascii | termK | fixRead |
|---|---|---|---|---|---|
| `dev:js` | 개발자 | JavaScript | ✅ | .30 | 3.5 |
| `dev:kotlin` | 개발자 | Kotlin | ✅ | .30 | 3.5 |
| `dev:swift` | 개발자 | Swift | ✅ | .30 | 3.5 |
| `dev:java` | 개발자 | Java | ✅ | .30 | 3.5 |
| `pm:policy` | 기획자 | 정책 | ❌ | **.62** | **4.0** |
| `design:design` | 디자이너 | 디자인 | ✅ | **.40** | **4.5** |
| `data:sql` | 데이터 분석가 | SQL | ✅ | .30 | **3.6** |

`localStorage['lot.pack']` 에 남고 `?pack=pm:policy` 로 덮어쓸 수 있다.

### 두 미션의 축을 분리해야 하는 이유

```
term(커밋)  정답을 보여준다   → 손 속도 승부
fix(오류)   정답을 숨기고 증상만 → 위치 판단 승부. 고칠 곳은 항상 한 군데
```

개발자판 `BUGS` 의 `err` 는 **틀린 토큰을 거의 그대로 알려준다**(12개 중 11개).
즉 fix 의 실제 난이도는 퍼즐이 아니라 *빨간 모니터까지 가는 동선 + 커서 이동*이다.

기획자판을 만들 때 이 친절도를 유지하지 않으면 난이도가 양쪽으로 튄다.
증상이 위치를 알려주면 타이핑량이 적어 **fix 가 term 보다 쉬워지고**,
증상을 모호하게 하면 par(`fixRead + 글자×.12`) 안에 못 끝내 **⚡보너스가 통째로 0**이 된다.
그래서 기획자판 증상도 "무엇이 잘못됐는지"까지 말해준다 — 콘솔 에러와 같은 친절도로.

기획자판 term 을 '문의 대응'으로 하면 **💬슬랙과 겹쳐 미션 세 개가 하나로 붕괴한다.**
그래서 term 자리는 문의 대응이 아니라 **확정·선언**(스펙 확정, 릴리즈 노트)이다.
서사적으로도 커밋은 "내 일을 확정해 남에게 넘기는 도장"이고 노트북 끄기가 여기 잠겨 있다.

### 디자이너 — 이름 vs 숫자

`term` 은 **레이어 네이밍**(`Button/Primary/Hover`), `fix` 는 **QA 제보에 따른 수치·규칙 수정**이다.
하나는 이름, 하나는 숫자라서 두 미션이 안 겹친다.

**CSS 속성 오타는 넣지 않는다** — `justify-content: cetner` 같은 건 퍼블리셔·프론트 일이지
디자이너가 치는 게 아니다. 처음에 이걸로 12슬롯을 짰다가 갈아엎었다.

`fix` 12개는 **디자이너만 아는 규칙**이다 — 대비 4.5:1 · 터치영역 44px · 다크모드 토큰 ·
4의 배수 스페이싱 · 타이포 스케일 · 포커스 링 · 비활성 투명도 · 전환 200ms ·
아이콘 세트 혼용 · radius 스케일 · 모달 z-index · 닫는 괄호.

퇴근 전 행위의 명분 — 이름 안 붙이고 가면 다음날 개발자가 Dev Mode 에서 `Frame 427` 만 본다.
`after:'commit'`(노트북 잠금)의 근거가 여기 있다.

> `publish library` 도 후보였지만 뺐다. Figma 퍼블리시 설명은 **선택**이라 실제로는 대부분
> 비워둔다. `git commit -m` 이 **필수**인 것과 전제가 다르다.

직군마다 **제보 채널**이 갈린다 — 개발자는 콘솔 에러, 기획자는 CS·정산 문의,
디자이너는 QA 리포트, 데이터 분석가는 지표 이상 감지.

### 문항은 D1 에 있다 — 고치는 데 배포가 필요 없다

**버그 20 · 커밋 10 · 슬랙 12**, 팩마다 (7팩 294행). `content` 테이블 한 개.
개수는 클라이언트의 `PACK_N` 상수와 DB 가 **같이** 지켜야 한다.

```sql
PRIMARY KEY (pack, kind, slot)     -- 슬롯 중복을 스키마에서 막는다
-- bug     a=bad  b=fix  c=err  d=tok
-- commit  a=문장
-- slack   a=질문  b=답장(이것만 타이핑한다)
```

`GET /content` 가 **전 팩을 한 번에** 준다(`cache-control: max-age=60`).
팩을 바꿀 때마다 재요청하지 않으려는 것이다. 부팅 때 한 번만 받는다.

**문항 원본은 `leaderboard-worker/content-source.json` 하나다.**
번들 배열(게임 파일)과 SQL 을 손으로 따로 쓰면 반드시 어긋난다.
둘 다 이 원본에서 생성하고, **게임 파일의 `PACKS` 를 실제로 실행해 덤프한 값**과
SQL 을 넣은 sqlite 결과를 항목 단위로 대조해서 확인한다.

**`slot` 순서가 곧 문항 순서다.** 서버가 `ORDER BY slot` 으로 주고 그 순서로 `rEvt()` 가 뽑으므로
같은 시드가 같은 문제를 낸다. 정렬을 바꾸면 재현성이 깨진다.

> **번들 배열(`SLACKS_DEV` 등, 팩 안의 `bugs`/`commits`)을 지우면 안 된다.**
> 서버가 죽거나 오프라인이면 그대로 폴백이다. `validPack()` 이 개수 계약(12/6/8)과
> 필수 필드를 검사해서 **한 군데라도 어긋나면 그 팩만 번들로 남긴다** —
> 우리가 SQL 을 잘못 넣어도 게임이 안 깨진다.

문항을 고치려면 SQL 한 줄이면 되고 GitHub Pages 캐시 10분도 안 기다린다.
단 **개수를 바꾸면 안 된다** — 12/6/8 이 아니면 폴백으로 되돌아간다.

> 아직 콘텐츠 리비전(`qrev`)은 없다. 데일리 챌린지를 만들 때
> 「어느 문항 버전으로 뛴 기록인가」가 필요해지면 그때 컬럼 하나 추가하면 된다.

### 💬 슬랙도 팩마다 다르다

팀장이 개발자에게 묻는 것과 디자이너에게 묻는 것은 다르다.
`SLACKS_DEV` / `SLACKS_PM` / `SLACKS_DESIGN` / `SLACKS_DATA` 각 12쌍이고 팩의 `slacks` 로 붙는다.

```
개발자    배포 언제 나가나요?      → 테스트 끝나면 나갑니다
기획자    이 정책 확정된 건가요?    → 오늘 안에 확정합니다
디자이너   시안 언제 볼 수 있나요?   → 오늘 중으로 공유합니다
```

**답장(뒤 문장)만 타이핑한다.** par 계수는 전 팩 공통(.36)이라 길이를 8~15자로 맞춰
par 이 3.9~6.4s 안에 들어오게 했다.

> ⚠️ **선언 순서에 걸린다.** `SLACKS_*` 는 `DEV_COMMON` **앞에** 있어야 한다.
> 뒤에 두면 `const` TDZ 로 로드 시점에 터지는데, **파싱 검사는 통과하기 때문에**
> `new Function()` 문법 검사로는 안 잡힌다. 실제로 한 번 그렇게 넣었다가 잡았다.

### 팩 규격 — 어기면 조용히 깨진다

1. `bugs` 정확히 **12개**, `commits` 정확히 **6개**. `rEvt()` 로 인덱싱하므로 길이가 다르면
   같은 시드가 팩마다 다른 문제를 낸다 → 데일리 챌린지·재현성 전제가 무너진다.
2. ~~같은 슬롯 번호는 팩끼리 같은 난이도~~ · ~~15~40자 밴드~~ — **완화됐다 (2026-08-26).**
   둘 다 팩끼리 점수를 비교하려던 규칙인데 순위표에 직군·언어 탭이 생겨 필요가 없어졌다.
   **이제 고증이 우선**이다. 다만 「전체」 탭은 여전히 전 팩이 섞이므로
   **파가 2배씩 벌어지지 않을 정도**의 느슨한 상한은 지킨다.
3. **새 팩을 넣으면 Worker 의 `ROLES`/`LANGS` 화이트리스트에 반드시 추가할 것.**
   빠뜨리면 `/score` 가 400 `bad lang` 을 내서 **그 팩 점수가 하나도 안 올라간다.**
   `pid` 때 겪은 사고와 같은 모양이다.
4. **새 팩을 넣으면 장비 12종 전부에 그 팩 키를 넣을 것.** 안 하면 4슬롯이 전부 죽는다.
5. `ascii:true` 팩은 본문에 **한글을 쓸 수 없다** — 입력창이 한글을 걸러낸다.
   단 증상(`err`)과 힌트(`tok`)는 표시용이라 한글이어도 된다.

실측 캘리브레이션 (평균 par):

| 팩 | fix | term | LV5 실측 typeSum |
|---|---|---|---|
| dev:js | 6.17s | 9.05s | — |
| dev:kotlin | 5.96s | 8.90s | 1,051 |
| dev:swift | 5.58s | 9.00s | 1,019 |
| pm:policy | 6.54s | 7.67s | 1,049 |

한글은 한 글자에 2~3 타 + 한/영 전환이 있어 `termK` 를 ASCII 의 두 배(.62)로 잡았다.
`fixRead` 4.0 은 정책문 한 줄 읽는 시간이다.

## 캐릭터 외형 (팔레트 스왑)

장비창(헤더 `👕 장비`)에서 헤어·상의·재킷·하의·신발 색을 고른다. **애셋 파일 추가는 0장이다.**

세 캐릭터 시트가 같은 베이스 리그를 쓰고 부위가 색으로 완전히 분리된다(24프레임 전부 확인).
그래서 기존 옷의 픽셀을 스텐실로 쓰면 정렬이 자동이다.

```
buildLook()   IMG.ch1 -> 오프스크린 canvas 에 색 치환 -> IMG.me
drawChar(meKey(), ...)  플레이어만 합성 시트로 그린다
```

**왜 캐시하는가** — 레이어를 매 프레임 겹치면 화면의 캐릭터 수만큼 비용이 붙는다.
외형이 바뀔 때 한 번만 구우면 `drawChar()` 는 지금처럼 `drawImage` 한 번이다.

**왜 `IMG.ch1` 을 직접 안 고치는가** — 순회 동료가 `ch1`/`ch2` 를 쓴다(`buildMap`).
원본을 치환하면 동료들 옷이 같이 바뀐다. 그래서 `IMG.me` 를 따로 만든다.

`has()` 는 `<img>` 와 `<canvas>` 를 모두 받도록 확장했다 — canvas 에는
`complete`/`naturalWidth` 가 없다.

### 색 램프

`RAMPS` 는 원본 팔레트(캐릭터 시트 3장 = 27색) 계열의 **3톤 램프**다. 어두운 → 밝은 순.
3톤 부위(헤어·상의)는 그대로 매핑하고 2톤 부위(재킷·하의·신발)는 램프의 `[0]`, `[2]` 만 쓴다.

| 부위 | 원본 색 (어두운 → 밝은) |
|---|---|
| 헤어 | `#3e3546` `#625565` `#7f708a` |
| 상의 | `#6e2727` `#b33831` `#fb6b1d` |
| 재킷 | `#0b8a8f` `#0eaf9b` |
| 하의 | `#4d65b4` `#4d9be6` |
| 신발 | `#45293f` `#753c54` |

아웃라인 `#2e222f` 는 **전 부위가 공유**하므로 램프에 넣으면 안 된다. 넣으면 실루엣이 무너진다.

### 의상 = 아이템 단위

`OUTFITS` 는 램프 인덱스 조합에 이름을 붙인 것이다 (기본 출근복·정장·고어룩·개발자 후드·
물류 조끼·연구원 가운·휴가룩·야근 좀비·신입 열정·컬리 퍼플 — 10종).
**이게 '갖고 있는 아이템' 단위이고, 나중에 세트 효과를 붙일 단위와 그대로 일치한다**
(정장 → 임원 위장 등, [`ITEMS-DESIGN.md`](ITEMS-DESIGN.md)).

장비창은 탭 두 개다 — `👔 의상`(카드 + 썸네일)과 `🎨 직접 배색`(슬롯별 65 스와치).
직접 배색이 어떤 프리셋과도 안 맞으면 `outfitId`가 `'custom'`이 되고 「직접 배색」으로 표시된다.

카드 썸네일은 전체 시트를 굽지 않고 **idle-아래 프레임 한 장(32×32)** 만 합성해서
`toDataURL()` 로 캐시한다. 10종이라 비용이 무의미하다.

**의상 색을 새로 만들 때는 재킷 색을 먼저 갈라라.** 재킷이 가장 넓은 면적이라
재킷이 겹치면 다른 옷으로 안 보인다. 실제로 개발자 후드를 고어룩과 같은 검정 재킷으로
잡았다가 구분이 안 돼서 회색으로 바꿨다.

### 코인과 상점

```
코인 = 레벨 클리어 점수 / 1,000        win() 에서 레벨마다 적립
```

런 총점의 1/1000 과 사실상 같지만 **레벨마다 즉시 들어온다.** 실패한 레벨은 0이다.
`?day=` 로 건너뛴 판은 최고기록과 같은 기준으로 제외한다.

실측 벌이 — 초보 한 판(LV3~4) 약 20코인, LV6 약 51, LV10 약 104, LV16 약 190.

| 의상 | 가격 |
|---|---|
| 기본 출근복 | 0 (기본 보유) |
| 개발자 후드 · 신입 열정 | 40 |
| 정장 · 고어룩 | 80 |
| 물류 조끼 · 연구원 가운 | 120 |
| 휴가룩 · 야근 좀비 | 200 |
| 컬리 퍼플 | 300 |

전부 사려면 1,180코인 = LV6 한 판 기준 약 23판. 첫 구매는 한두 판이면 된다.

| 소모품 | 가격 | 보유 상한 |
|---|---|---|
| 🤖 Claude Code | 10 | 5 |
| 💼 사직서 | 20 | 5 |

**소모품은 값이 아니라 `STOCK_CAP` 으로 막는다.** 코인은 영구 누적이라 값으로는 못 막는다 —
비싸게 두면 초보만 막히고 100판 한 사람은 어떤 값이든 산다. 개수로 막으면 둘 다 5개다.
값을 싸게 둔 이유는 한 번 쓰면 사라지는 물건이라 최저가 영구템(의상 40)보다 싸야 하기 때문이고,
🤖10 < 💼20 인 이유는 **야근 확정이 런 자체를 끝내기** 때문이다 (`ITEMS-DESIGN.md` 「사직서」).

`STOCK_CAP` 은 **세 곳**이 다 봐야 한다 — `SHOP.buy()`(구매 차단) · 재고 로더 ·
`SHOP.apply()`(서버 지갑). `apply()` 를 빼먹으면 상한 도입 전에 9개를 갖고 있던 지갑이
서버에서 그대로 되돌아온다.

### 지갑은 pid 기준으로 서버에 있다

코인·아이템은 **진행도**다. 점수 한 줄 날아가는 것과 체감이 다르다.
그래서 `localStorage` 와 D1 `wallet` 양쪽에 둔다.

```
GET  /wallet?pid=...             { rev, data }        없으면 rev:0, data:null
POST /wallet {pid, rev, data}    { ok:true, rev }
```

**서버는 아이템 카탈로그를 모른다.** `data` 는 통 JSON 이고 서버는 크기(4KB)와 형식만 본다.
컬럼으로 쪼개면 의상·장비를 추가할 때마다 클라이언트와 Worker 를 같이 고쳐야 하는데,
그건 이 저장소가 이미 데인 함정이다(`CLAUDE.md` 1번).

`rev` 는 단조 증가한다. 서버는 `rev` 가 자기 값 **이상**일 때만 덮어쓰므로
오래 열려 있던 탭이 최신 지갑을 되돌리지 못한다.
클라이언트는 저장할 때마다 `rev++` 하고 1.2초 디바운스로 올린다(연속 구매를 한 번으로 모은다).
오프라인이면 조용히 넘어가고 게임은 계속된다.

> ⚠️ **위조는 막지 못한다.** 점수와 같은 클라이언트 신뢰 모델이다.
> 목적은 무결성이 아니라 **영속성**이다.

### 🔑 기기 이동 코드 — 서버 지갑의 짝

**서버에 지갑을 두는 것만으로는 반쪽이다.** `pid` 도 `localStorage` 에만 있어서
캐시를 지우면 pid 가 같이 사라지고 그 지갑에 다시 닿을 수 없다.

그래서 장비창 푸터의 `🔑 기기 이동` 에서 **pid 자체를 코드로 보여주고 붙여넣을 수 있게** 했다.
다른 브라우저에서 코드를 넣으면 지갑과 순위표 기록을 그대로 이어받는다.

> ⚠️ 이 코드는 **사실상 비밀번호**다. 남에게 보여주면 그 사람이 지갑과 기록을 가져간다.
> 화면에도 그 경고를 같이 띄운다.

DEV 모드(`?dev=1`)는 지갑을 동기화하지 않는다 — 치트 지갑이 서버에 올라가면 안 된다.

인벤토리 도입 전 기록은 **기본 출근복 + 그때 입고 있던 옷**을 보유로 인정한다. 빼앗지 않는다.

장비창 탭은 넷이다 — `👔 의상` · `🎒 장비` · `🛒 상점` · `🎨 배색`.

### 장비템 — 슬롯당 하나, 팩별 효과

⌚워치 · 💻노트북 · 📱태블릿 · 🎧이어폰 네 슬롯, 12종.
같은 슬롯은 하나만 착용한다(같은 것을 다시 누르면 해제).

효과는 `⌨️커밋`·`🚨오류수정`의 **par(⚡보너스 기준시간)을 늘리는** 방식이다.
보너스 상한(fix 700 / term 600)이 그대로 걸려 무한히 세지지 않고,
합산 배율을 `GEAR_CAP = 1.6` 으로 한 번 더 자른다. 슬랙·설거지에는 적용되지 않는다.

#### 배분 원칙 — 새 장비를 넣을 때 반드시 지킬 것

1. **모든 팩이 모든 슬롯에서 0 보다 큰 값을 받아야 한다.** 하나라도 0 이면 그 팩은
   그 슬롯에 선택지가 없다(죽은 슬롯). 실제로 기획자의 워치 슬롯과 JS 의 태블릿 슬롯이
   이렇게 죽어 있었다 — 아래 검사로 잡았다.
2. **장비는 「전용」과 「범용」 둘 중 하나다.**
   전용은 한쪽 생태계가 **0** 이고(애플워치 → Kotlin 0), 범용은 전 팩이 같은 값을 받는다
   (가민워치·그램·오버이어 헤드셋). 이 구분이 아래 검사의 기준이다.
3. **JS 는 전용 장비에서 언제나 중간값이다.** 애플 제품이면 Swift 최고 · JS 중간 · Kotlin 0,
   삼성 제품이면 그 반대. 대략 전문 팩의 절반이다.
   잘못 사는 일이 없는 대신 **상한에 못 닿는다**(×1.49).
4. **전문 팩(Kotlin·Swift)은 자기 생태계에서만 크게 받고 상한까지 간다**(×1.60).
   Kotlin 이 애플워치를 사면 **0** 이다. 그게 선택의 무게다.
5. **기획자는 생태계 종속이 없다** — 워치는 캘린더, 노트북은 문서 도구라 브랜드를 안 가린다.
   대신 최고치는 전용 장비(그램 +30%, 갤럭시 폴드 +30%, 헤드셋 +25%)가 갖는다.

| 팩 | 슬롯별 최선 | 풀착용 |
|---|---|---|
| JS | 애플/삼성워치 +12% · 맥북/씽크패드 +15% · 아이패드/갤탭 +12% · 헤드셋 +10% | ×1.49 |
| Kotlin | 삼성워치 +25% · 씽크패드 +30% · 갤탭 +25% · 버즈 +10% | ×1.60 |
| Swift | 애플워치 +25% · 맥북 +30% · 아이패드 +25% · 에어팟 +10% | ×1.60 |
| 기획자 | 워치 +12% · **그램 +30%** · **갤럭시 폴드 +30%** · **헤드셋 +25%** | ×1.60 |
| Java | 워치 +10% · **씽크패드 +30%** · 태블릿 +8% · **헤드셋 +25%** | ×1.60 |

Java 는 **노트북과 이어폰에 몰린다** — 백엔드는 빌드 머신과 집중 환경이 핵심이고
태블릿은 거의 안 쓴다. 워치는 생태계 중립(전 브랜드 +10%)이다.

**죽은 슬롯 검사** — 장비를 추가·수정했으면 이걸 돌릴 것:

```bash
# 모든 팩 × 모든 슬롯에 0 보다 큰 선택지가 있는지
python3 - <<'EOF'
import io,re,json
s=io.open('leave-on-time/index.html',encoding='utf-8').read()
g=s[s.index('const GEAR = ['):s.index('const SLOTS = {')]
items=re.findall(r"slot:'(\w+)',\s*p:\d+,\s*boost:\{([^}]*)\}", g)
slots=sorted({i[0] for i in items})
for L in ['js','kotlin','swift','policy']:
    for sl in slots:
        best=max((float(re.search(L+r':(\.\d+)',b).group(1)) if re.search(L+r':(\.\d+)',b) else 0)
                 for s2,b in items if s2==sl)
        print(('OK  ' if best>0 else '죽음'), L, sl, best)
EOF
```

### 소비템 — 쓸 때마다 사라진다

| | 가격 | 효과 |
|---|---|---|
| 🤖 Claude Code | 300 | `🚨오류수정`·`⌨️커밋` 중 **Tab** — 즉시 고치고 전송 |
| 💼 사직서 | 200 | 야근 확정 1회 방어 — 그 날을 처음부터 다시 |

`💬슬랙`과 `🧽설거지`는 Claude 대상이 **아니다.** 셋 다 되면 게임 전체를 대신해버린다.

Claude 안내는 **입력창 힌트 줄**에 칩으로 띄운다(보유 중 + fix/term 일 때만).
사이드 HUD 배지는 타이핑 중에 눈이 안 가서 사람들이 아이템을 인지하지 못한다.

> ⚠️ 힌트 줄을 `innerHTML` 로 덮어쓰는 곳이 **셋**이다 — 미션 시작 · 6초 힌트 · 한글 IME 경고.
> 각자 칩을 붙이면 반드시 한 군데서 빠진다. `hintTail()` 한 곳에서만 만들 것.

**사직서는 자동으로 소모되지 않는다.** 마감을 넘기면 `state = 'resign'` 인 선택 화면이 뜨고
`💼 사직서 제출` / `🌙 그냥 야근한다` 중 고른다. 200코인짜리를 묻지 않고 태우면 안 된다.

`'resign'` 을 별도 state 로 두는 이유 — `'playing'` 이 아니어야 `update()` 가 멈추고
(`if (state !== 'playing') return`), `'gameover'` 가 아니어야 `lose()` 의 정산·기록 제출이 아직 안 일어난다.
이 화면에서는 장비창·게시판을 못 연다(`locked()`) — 결정 중에 상점에서 사직서를 더 사면
화면에 찍힌 「남은 N장」과 실제 보유량이 어긋난다.

사직서는 `R` 키와 같은 경로다 — `startWave(wave)` 라서 **시드가 유지되고 같은 배치로 재도전**한다.
`lose()` 는 점수를 더하지 않으므로(`score += br.total` 은 `win()` 에만 있다) 계산이 안 꼬인다.
대신 `levelFree` 플래그가 서서 **그 레벨은 클리어·시간 보너스를 못 받는다** — 야근한 날은 정시 퇴근 점수가 없다.
`startWave` 에서 초기화하므로 다음 레벨엔 영향이 없다.

`localStorage['lot.baseLook']`(기본 출근복 배색) + `['lot.outfit']`(의상 id)에 저장한다.
다만 **라벨은 저장값을 믿지 않고 `look` 에서 다시 유도한다** — 의상 개념이 없던 판(색만 저장)에서
넘어온 기록도 이름이 맞아야 하기 때문이다.

## localStorage 키

브라우저에 남는 전부다. **이 중 `lot.pid` 가 사라지면 계정 자체가 사라진다** —
서버의 지갑·순위표 기록에 다시 닿을 수 없다(「🔑 기기 이동 코드」 참고).

| 키 | 내용 |
|---|---|
| `lot.pid` | 숨은 고유 ID. **이게 계정이다** |
| `lot.nick` | 표시용 닉네임 |
| `lot.bestLv` `lot.bestScore` | 개인 최고 기록 |
| `lot.board` `lot.posts` | 서버가 죽었을 때 쓰는 순위표·게시판 폴백 |
| `lot.pack` | 고른 직군·언어 팩 |
| `lot.outfit` `lot.baseLook` | 착용 의상 · 기본 출근복 배색 |
| `lot.coins` `lot.owned` `lot.stock` `lot.gear` | 지갑·인벤토리 |
| `lot.rev` | 지갑 리비전. 서버 동기화의 기준 |

`lot.pack` 을 뺀 나머지 지갑 관련 값은 `wallet` 테이블에도 통 JSON 으로 올라간다.

## 신원 모델

```
pid   숨은 고유 ID. D1 기본키. 신규는 16자 랜덤 hex
nick  표시용 라벨. 언제든 바꿀 수 있다 (2~10자 + #16진수3자)
꼬리  pid 앞 3자리에서 파생 — 이름을 바꾸거나 🎲를 돌려도 유지되는 식별 배지
```

**닉네임을 키로 쓰면 이름을 바꿀 때마다 새 행이 생겨 같은 사람이 중복된다.** 그래서 분리했다.

pid 도입 전 사용자는 `닉네임 전체의 UTF-8 hex`로 pid를 유도해 옛 기록에 이어붙는다
(SQLite `lower(hex(nick))`와 동일). **앞부분만 잘라 쓰면 안 된다** — 한글은 글자당 3바이트라
16자로는 2.7자밖에 안 담겨 앞 글자가 겹치는 닉네임끼리 충돌한다(실제로 한 명의 기록이 사라졌다).

## API (Cloudflare Worker + D1)

```
GET  /top?n=10&pid=...&role=&lang=  { rows:[{nick,lv,score}], myRank, total }
POST /score {pid,nick,lv,score,seed,role,lang}
GET  /posts?n=30&before=<id>      커서 페이지네이션 (무한스크롤)
POST /post  {pid,nick,body}
POST /post/del {pid,id}           pid 일치하는 글만 삭제
GET  /content                     { packs: { 'dev:js': {bugs,commits,slacks}, ... } }
GET  /wallet?pid=...              { rev, data }  없으면 rev:0, data:null
POST /wallet {pid,rev,data}       { ok:true, rev }  rev 가 서버 값 이상일 때만 덮어씀
```

### 순위표 탭

`scores` 는 손대지 않았다 — **`pid` 당 한 행(역대 최고점) = 「전체」 탭**이다.
한 사람이 여러 팩을 뛸 수 있으므로 팩별 최고점은 별도 테이블에 남긴다.

```sql
pack_scores  PRIMARY KEY (pid, role, lang)     -- migrate-004-pack.sql
```

탭은 **전체 · JS · Kotlin · Swift · 기획자** 다섯 개다. 개발자 탭은 세 언어의 합집합이라 뺐다.
언어 탭은 `lang=`, 기획자 탭은 `role=pm` 으로 거른다 — 기획자에 도메인(커머스·물류·정산)이
늘어도 그 탭이 전부 모아주도록 `role` 쪽으로 잡았다.

`/top` 에 `role=` 또는 `lang=` 이 오면 `pack_scores` 를, 없으면 `scores` 를 본다.
`myRank` 도 같은 필터 안에서 센다.

**팩 도입 전 42행은 `migrate-004` 가 `dev:js` 로 백필한다.** 그 시절 커밋 문장도 버그 코드도
JavaScript 판 하나뿐이었으므로 추정이 아니라 사실이다. 클라이언트의 `readBoard()` 도
role 이 없는 로컬 행을 같은 기준으로 `dev:js` 로 읽는다 — **두 기준이 어긋나면 서버가
살아있을 때와 죽었을 때 다른 목록이 나온다.**

팩이 여러 개가 된 뒤로는 이 가정을 절대 쓸 수 없다. `role`/`lang` 이 빠진 제출은
(옛 클라이언트 캐시) 「전체」에만 들어간다.

`scores` 의 PK 를 `(pid, role, lang)` 으로 바꾸는 방법이 더 정규적이지만
살아있는 42행을 재생성해야 해서 택하지 않았다.

클라이언트의 `LBTABS` 는 서버 필터(`q`)와 로컬 폴백 필터(`f`)를 **한 줄에 나란히** 둔다.
한쪽만 고치면 서버가 살아있을 때와 죽었을 때 다른 목록이 나와 버그로 보이지 않는다.

서버 검증: 오리진 화이트리스트 · pid/닉네임 형식 · `role`/`lang` 화이트리스트 ·
`role`·`lang` 은 둘 다 있거나 둘 다 없어야 함(옛 클라이언트 호환) · 레벨 1~99 ·
점수 개연성(`20000×LV + 250×LV×(LV+1)` 초과 거부) · 레이트리밋(점수 5초, 글 10초, IP는 해시만 저장).

**클라이언트 조작을 원천 차단할 수는 없다.** 정적 사이트라 개연성 범위 안의 가짜 점수는 넣을 수 있다.
완전 차단은 시드 + 입력 로그를 보내 서버가 재시뮬레이션해야 하고, **시드 기반 난수는 그 준비가 되어 있다.**

클라이언트는 `LB` / `BOARD` 래퍼 뒤에 있어서 **`LB.url` 한 줄만 바꾸면 백엔드를 갈아끼울 수 있다.**
서버가 죽으면 자동으로 localStorage 기록으로 대체되어 게임이 멈추지 않는다.

## 다음에 할 만한 것

1. **데일리 챌린지** — 시드 기반 난수가 이미 있으므로 Worker가 그날의 시드를 내려주기만 하면 된다.
   실시간 없이도 "오늘 배치에서 누가 제일 빨리 나갔나" 경쟁이 성립한다. 가장 값싸고 효과가 크다.
2. **LV13+ 단조로움 해소** — 지금은 마감만 줄어든다.
   할 일 7개 중 5개를 시드로 뽑으면 같은 레벨도 매번 동선이 달라진다.
2-b. **팩 추가** — 디자이너(피그마 코멘트 반영 / 컴포넌트 네이밍 오류)나
   데이터 분석가(쿼리 오타 / 지표 정의 오류)는 위 규격만 지키면 콘텐츠 팩 하나로 끝난다.
3. **실시간 멀티플레이** — 같은 계정·프로젝트에 **Durable Objects** 바인딩만 추가하면 된다.
   방 하나 = 인스턴스 하나(싱글스레드)라 경쟁 조건이 없고, WebSocket을 직접 물릴 수 있다.
   마이그레이션 없이 확장 가능한 게 Cloudflare를 고른 이유다.
4. **화장실·사물함 외 지점에도 목표 아이콘** — 초반 위치 학습에 도움.

5. **아이템·캐릭터 설정** — 설계안이 [`ITEMS-DESIGN.md`](ITEMS-DESIGN.md)에 있다 (미구현).
   애셋은 PNG 추가 0장으로 시작할 수 있다(팔레트 스왑 + 방향별 3장 도트 모자).
   **아이템 효과를 붙이기 전에 그 문서의 「계산해서 알아낸 함정」을 읽을 것** —
   엘베 문 열리는 시간은 이미 공짜라 점수 영향이 0이고, `DISH_GAIN`을 건드리면 난이도가 폭발한다.

## 알려진 한계

- 순위표 점수는 클라이언트가 만든다 (위 참고).
- 디버그 훅(`__tp` `__clock` `__coin` `__giveAll` `__dbg`)은 `?dev=1` 에서만 정의된다.
  **숨기는 게 방어가 아니라** DEV 인 런이 순위표 제출과 코인 적립을 모두 건너뛰는 게 방어다.
  소스를 읽고 `localStorage` 를 직접 고치는 것까지는 막지 못한다.
- **장비·소비템이 점수에 영향을 준다.** 장비는 par 을 늘려 ⚡보너스를 키우고,
  사직서는 죽었어야 할 런을 이어준다. 순위표는 이걸 구분하지 않는다.
  구분하려면 `pack_scores` 에 컬럼(`bare` 또는 `resign` 횟수)을 추가하고
  Worker·D1 을 함께 배포해야 한다 — 지금은 안 했다.
- 닉네임 중복을 막지 않는다. `#꼬리`로 구분된다. 욕설 필터 없음.
- 게시판에 신고/관리자 기능이 없다. 본인 글 삭제만 가능.
- Worker 점수 검증이 `lv ≤ 99`다. 난이도를 크게 완화하면 상한에 걸릴 수 있다.
